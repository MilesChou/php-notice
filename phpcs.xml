<?xml version="1.0"?>
<ruleset name="Common library coding standard">
    <description>Common library coding standard</description>

    <!-- display progress and sniff -->
    <arg value="p"/>
    <arg value="s"/>

    <!-- use colors in output -->
    <arg name="colors"/>

    <!-- inherit rules from: -->
    <rule ref="PSR2"/>

    <!-- Paths and file to check -->
    <file>tests</file>
</ruleset>
